<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Ashevillagers" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Ashevillagers">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=IM+Fell+DW+Pica:ital@0;1&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <link rel="icon" href="/favicon.ico" sizes="any">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">

    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application' %>
  </head>

  <body class="bg-white min-h-screen font-sans">
    <nav class="bg-[#2672B5] text-white">
      <div class="mx-auto px-4 py-3 flex items-center justify-between container gap-2">
        <%= link_to root_path, class: "block relative -top-[1px]" do %>
          <img src="/images/brr-logo-border.svg" alt="Blue Ridge Ruby" class="h-10">
        <% end %>

        <div class="flex items-center gap-4 md:gap-6 text-base font-medium relative top-[1px]">
          <span class="hidden sm:inline">April 30 &amp; May 1, 2026 &middot; <a href="https://blueridgeruby.com/#register" class="text-[#ADD6F2] underline text-nowrap">Register Now</a></span>
          <% if current_villager %>
            <%= link_to edit_profile_path, class: "border border-white rounded px-3 py-1.5 font-medium hover:bg-white/10 transition whitespace-nowrap flex gap-1.5 items-center" do %>
              <%= current_villager.profile&.finalized? ? "Update" : "Create" %> <%= phosphor_icon(:user, style: "bold", class: "inline w-5 h-5") %>
            <% end %>
          <% else %>
            <%= link_to "Login", new_session_path, class: "border border-white rounded px-4 py-1.5 font-medium hover:bg-white/10 transition" %>
          <% end %>
        </div>
      </div>
    </nav>

    <% if notice %>
      <div class="max-w-6xl mx-auto px-4 mt-4">
        <div class="p-3 rounded bg-green-100 text-green-800"><%= notice %></div>
      </div>
    <% end %>
    <% if alert %>
      <div class="max-w-6xl mx-auto px-4 mt-4">
        <div class="p-3 rounded bg-red-100 text-red-800"><%= alert %></div>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
